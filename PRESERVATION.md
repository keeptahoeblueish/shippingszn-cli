# Preserved unfinished work

This branch is for recovery of unfinished work only. It is not a reviewed release or a deployment candidate.

Original source commit: `247ed989348e9b80e9fe57af52f19ca2f574cf82`

The original commit is this guard commit's sole parent. Its complete original tree remains recoverable by checking out that SHA in a separate checkout. This child adds only this note and disables automatic Vercel Git deployments at the configured project roots. Any original configuration remains in the parent.

Recorded branch sources:

```json
[
  {
    "kind": "checkout",
    "branch": "main"
  },
  {
    "kind": "ref",
    "ref": "refs/heads/main"
  }
]
```
